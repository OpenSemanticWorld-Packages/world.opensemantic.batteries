{ 
    "title": "{{{name}}}",
    "defaultProperties": [
        "label"{{#if battery_cell_form_factor}}, 
        "battery_cell_form_factor"{{/if}}
    ],
    "required": [
        {{#if battery_cell_form_factor}}"battery_cell_form_factor"{{/if}}
    ],
    "properties": { 
        {{#if battery_cell_form_factor}}battery_cell_form_factor": {
            "default": "{{{battery_cell_form_factor}}}"
        } {{/if}}
    } 
}